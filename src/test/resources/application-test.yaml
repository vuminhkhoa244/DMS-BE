spring:
  datasource:
    url: jdbc:h2:mem:document_management_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  mail:
    host: localhost
    port: 2525
    username: test
    password: test

app:
  jwt:
    secret: test-secret-key-test-secret-key-test-secret-key
  cors:
    allowed-origin-patterns: http://localhost:*
  admin:
    seed:
      enabled: false

cloudinary:
  cloud-name: test
  api-key: test
  api-secret: test
